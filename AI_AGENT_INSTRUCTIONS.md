# JobWatcher — Инструкция для AI-агента

**Формат:** Markdown. Поместить файл в корень репозитория как AI_AGENT_INSTRUCTIONS.md.
**Цель:** пошаговый, приоритетный и безопасный план рефакторинга/улучшений прототипа (минимальные изменения, проверка работоспособности после каждого шага).

---

## Краткое описание системы (контекст)

JobWatcher — прототип на Python, который собирает вакансии из Telegram-каналов и фильтрует их по заданным маркерам. Основные входы/выходы:

- Вход: список каналов (в config.yaml), период сканирования (CLI), Telegram API (Telethon).

Главный алгоритм:

Запустить Telethon клиент.
- Сканировать указанные каналы за заданный период (scanner.scan_history / tg_job_scan.py).
- Для каждого сообщения вызвать score.score_and_classify(text, cfg) — вычислить совпадения, суммы, итоговую метку.
- Сохранять/помечать сообщения в SQLite (seen таблица).
- Сформировать пакет результатов и отправить в целевой канал (sender.send_results / tg_job_scan.py).
- Логировать статистику: найдено, принято, отброшено, распределение по меткам.

Основные файлы (текущее состояние репо):

- score.py — нормализация/стемминг и функция score_and_classify.
- tg_job_scan.py — CLI/сканирование/формирование результатов/отправка (сейчас смешивает обязанности).
- tg_job_watcher.py — watch-mode: живое прослушивание каналов, inline-кнопки, изменение статусов.
- config.yaml — маркеры, thresholds, список каналов.
- jobwatcher.db — sqlite runtime БД (не хранить в репо).

--- 

## Главные ограничения и требования (сформулированы пользователем)

1. Игнорирование/стоп: сообщение должно быть немедленно отброшено (stop processing, final_score = None) только если найден маркер из ignore_markers (например "реклама"). В остальных случаях считаем все маркеры и итоговый final_score = positive_sum - negative_sum.

2. Счёт:

- Отличные маркеры = +2 (или configurable).
- Допустимые = +1.
- Негативные = -1 (суммируются в negative_sum как положительная величина для отчёта).
- Резко негативные = -2 (учитываются в сумме, но не автоматически сбрасывают итог, если только они не в ignore_markers).

1. Нормализация слов: учёт регистра, знаков (#qa), падежей (русские формы) — реализуется стеммингом/нормализацией. YAML содержит базовые слова; код должен сравнивать по нормализованным стемам.
1. Сохранность поведения: рефакторинг выполняется по-шагово; после каждого шага запускается проверка (unit tests / test_score.py / python tg_job_scan.py) — критично сохранить текущую функциональность прототипа.
1. Минимальные diffs и сохранение стиля: изменения только в целевых местах; не менять порядок переменных/методов, пробелы/комментарии вне областей правки; не делать стилистических правок иначе.
1. Логика отправки: Telegram лимит ≈4096 символов; батчинг должен учитывать это и не посылать пачку, превышающую лимит; длинные отдельные посты аккуратно обрезать с ….
1. Тестируемость: собрать лёгкую проверку после каждого изменения — python test_score.py, затем python tg_job_scan.py (режим scan), проверка отправки в тестовый канал.
1. База данных: не удалять данные без явного указания; при тестировании можно временно удалить jobwatcher.db чтобы вернуть начальное состояние.
1. Документировать изменения: каждая правка коммитится с коротким описанием и номером шага.

---

## Приоритетный и пошаговый план улучшений (порядок выполнения, минимальные изменения, проверка работоспособности)

> Каждая задача — атомарная. После выполнения шага: запустить автотесты + ручную проверку (см. раздел «Проверка и валидация»). Если тесты не проходят — откатить/исправить до совпадения результатов.

### Шаг 0 — Подготовка (прежде чем менять бизнес-логику)

Цель: гарантировать быстрый возврат при ошибках.

- Создать ветку refactor/step-00-prepare.
- Добавить requirements.txt (nltk, telethon, pyyaml, python-dotenv) если ещё нет.
- Добавить .gitignore правила: исключить jobwatcher.db, *.session, __pycache__.
- Создать AI_AGENT_INSTRUCTIONS.md (этот файл) в корне.
- Проверка: python test_score.py должен работать, python -c "import tg_job_scan" — импортироваться без ошибок.

Почему: подготовка позволяет откатить любые изменения.

### Шаг 1 — Вынести loader конфигурации (low risk)

Цель: собрать всю логику чтения .env и config.yaml в один модуль.
Действия:

1. Создать config_loader.py с функцией load_config():
- вызывает load_dotenv() и делает cfg = yaml.safe_load(open("config.yaml"));
- валидирует TG_API_ID, TG_API_HASH, TARGET_CHAT_ID присутствуют;
- возвращает dict { "env": {...}, "cfg": cfg }.
1. В tg_job_scan.py и tg_job_watcher.py заменить прямые os.getenv/load_dotenv на from config_loader import load_config; conf = load_config().
1. Не изменять имена переменных, только точку доступа.

Проверка:
python test_score.py и python tg_job_scan.py (короткий прогон) — поведение не должно измениться.
Ограничение: не менять формат возвращаемого cfg — функция должна быть drop-in.

### Шаг 2 — Вынести DB wrapper (medium risk)

Цель: инкапсулировать sqlite операции.

Действия:

1. Создать db.py с API:
- init_db(path): создаёт таблицу seen (с текущими колонками).
- get_seen_by_unique(unique), insert_seen(record), update_status(unique, status).
2. В tg_job_scan.py / tg_job_watcher.py заменить прямые SQL вызовы на эти функции.

Проверка:
Повторный прогон python tg_job_scan.py, проверить, что результаты и БД идентичны (новая БД создаётся при первом запуске).
Ограничение: схемы таблиц и имена колонок не менять.

### Шаг 3 — Вынести форматирование постов (low risk)

Цель: отделить текстовые представления от логики отправки.
Действия:

1. Создать formatter.py:

- format_post_block(item) — возвращает строку, форматированную по текущим правилам (эмодзи-шкала, ссылки, разделители).
- build_emoji_bar(score, max_slots=3) — возвращает 🟩/🟨 строку (с учётом шкалы 0..3).
- Докстринги указывать, но не менять вид форматирования по умолчанию.

2. В tg_job_scan.py заменить inline-formatting на from formatter import format_post_block.

Проверка:
- Формативные строки в канале/в тесте не изменились семантически.
Ограничение: не менять шаблон вывода без отдельного шага согласования.

### Шаг 4 — Реализовать sender.py с безопасным батчингом (high value, medium risk)

Цель: убрать баги Message was too long, логировать фактические отправки.
Действия:

1. Создать sender.py с async def send_results(client, results, target_chat, pause_sec=1.2, message_limit=4000):

- отправлять стартовый заголовок (время, период, количество найденных, распределение по меткам);
	- для каждого item:
	- получить block = format_post_block(item);
	- если len(block) > message_limit: аккуратно обрезать preview поля (или весь block) так, чтобы len(block) <= message_limit, и логировать обрезку;
	- батчить: если len(current_batch) + len(block) > message_limit → отправить current_batch; затем начать новый.
- сохранять счётчики sent, failed и логировать реальные результаты. Пауза await asyncio.sleep(pause_sec) между отправками.

2. В tg_job_scan.py заменить вызов старой локальной send_results на from sender import send_results.


Проверка:
Интеграционный прогон: python tg_job_scan.py на небольшом периоде; в логах не должно быть Message was too long. Сравнить sent счетчик в финальном отчёте с сообщениями в канале.
Ограничение: не менять формат одиночного block кроме обрезки preview для очень длинных постов.

### Шаг 5 — Вынести scanner (medium risk)

Цель: разделить сбор сообщений и отправку.
Действия:
1. Создать scanner.py с async def scan_history(client, channels, hours, db, cfg) -> List[items]:
- функция должна проходить по channels и формировать results в том же формате, что tg_job_scan.py сейчас использует (канал, msg_id, final, pos, neg, summary, preview).
- использовать db wrapper (шаг 2) для проверки дублей/записи.

2. В tg_job_scan.py оставить CLI/runner, который вызывает scan_history и send_results.

Проверка:
Вызов python tg_job_scan.py должен вернуть те же результаты, что до рефакторинга.
Ограничение: интерфейс scan_history должен сохранять ту же структуру results.

### Шаг 6 — Рефактор watcher (low-medium risk)

Цель: держать live-режим в отдельном модуле.
Действия:

1. Вынести логику Telethon event handler в watcher.py (перенести tg_job_watcher.py в watcher.py, минимальные правки на использование config_loader и db).
1. Убедиться, что inline-кнопки и обработчики callback работают как прежде.
Проверка: запустить watcher локально (в тестовом аккаунте) и проверить: приход новых сообщений, кнопки меняют статус в БД, отображение в целевом чате.
Ограничение: не менять callback payloads (формат apply:msgid:channel), не менять статусы text.

### Шаг 7 — Кэширование нормализованных маркеров в score.py (low risk)

Цель: снизить CPU-затраты и убрать повторный стемминг.
Действия:

1. В score.py при импорте/инициализации собрать нормализованные карты _build_norm_map() один раз и хранить как модульную переменную.
1. score_and_classify должен использовать уже подготовленные норм-мапы вместо пересчёта для каждого сообщения.
Пр
оверка:

- Тест скорости: многократный запуск score_and_classify для N сообщений должен ускориться.
- Функциональность (результат) не должна меняться.
Ограничение: не менять названия маркерных ключей; кэш должен инвалижиться при явной перезагрузке конфига (не требуется в рамках этого шага).

### Шаг 8 — Добавить CI проверки и unit tests (low risk, высокое значение)

Цель: обеспечить автоматический контроль работоспособности после каждого шага.
Действия:

- Добавить tests/test_score.py (проверки на примерах: падежи, #qa, automation, strong_negative не стоп).
- В GitHub Actions добавить job pytest и flake8 (опционально).
- Добавить job agent-check который запрещает PR без AI_AGENT_INSTRUCTIONS.md и без прохождения тестов.
- Проверка: PR с изменениями не проходит pipeline → откат/исправление.


---

### Проверка и валидация после каждого шага

- Unit tests: pytest (score tests).
- Smoke test:
- python test_score.py — вручную проверить результаты.
- python tg_job_scan.py — сканирование за короткий период (24 часа) и наблюдение логов: количество найденных / отправленных / упавших.
- Manual inspection: зайти в тестовый целевой канал — проверить, что формат сообщений соответствует предыдущему состоянию (за исключением аккуратных обрезок).
- DB integrity: проверить, что seen таблица не потеряла старых записей (если не было намеренного удаления).

---

### Тонкости реализации (важные детали)

- Не менять поведение скоринга: точные веса/пороговые значения должны быть параметризованы, но пока их брать из config.yaml или оставить дефолты. Любые изменения порогов — отдельный шаг с тестированием.
- Ignore markers: это единственная ветка, где обработка сообщения немедленно завершается с final_score=None. (Как требовал пользователь.)
- Strong negative: считается как обычный отрицательный вес (не приводит к автоматическому None).
- Структура results: сохранять поля: channel, msg_id, final, pos, neg, summary, preview, raw_text (по необходимости) — чтобы sender имел все данные.
- Сообщения Telegram: проверять длину итогового блока в байтах/символах перед отправкой; использовать безопасный предел 4000 символов (параметризуемая переменная MESSAGE_LIMIT).
- Изменения в файлах: минимальные diffs; не трогать другие области кода; сохранять последовательности объявлений.

---

### Порядок вызова главного алгоритма 

1. CLI → run_scan (или python tg_job_scan.py)
1. config_loader.load_config() → вернуть env + cfg
1. Инициализация DB: db.init_db(path)
1. Инициализация TelethonClient
1. scanner.scan_history(client, channels, hours):
    - для каждого msg: text = extract_text(msg); res = score.score_and_classify(text, cfg); db.insert_seen(...); append to results if relevant
1. sender.send_results(client, results, target_chat_id, pause_sec, message_limit)
	- отправить заголовок
	- для каждого item: build block via formatter.format_post_block; if block>LIMIT → truncate preview; batch and send
	- отправить финальный отчёт
1. Завершить TelethonClient


Конец инструкции